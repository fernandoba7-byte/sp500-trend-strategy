name: Daily Signal Check
on:
  schedule:
    - cron: '0 22 * * 1-5'  # 5:00 PM ET / 4:00 PM CT (1hr after market close)
  workflow_dispatch:          # Allow manual trigger

permissions:
  contents: write

jobs:
  check-signal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run daily signal check
        run: python monitor/daily_check.py
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_RECIPIENT: ${{ secrets.EMAIL_RECIPIENT }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}

      - name: Commit signal log and dashboard
        run: |
          git config user.name "Signal Bot"
          git config user.email "bot@signal"
          git add data/signal_log.csv data/status.json docs/data.json
          git diff --staged --quiet || git commit -m "Signal update $(date +%Y-%m-%d)"
          git push
